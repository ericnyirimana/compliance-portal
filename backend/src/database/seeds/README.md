# Seeds

Run with: `npm run seed` from the backend directory.

The seed is idempotent — it checks for existing users before inserting.
It creates all users, sample applications, and a valid audit hash chain.
